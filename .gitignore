# IdlerGear data
.idlergear/

# Claude Code data
.claude/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Data files (not in git - too large) — EXCEPT the QA test sets, which are
# small and belong under version control so they can be reviewed/reproduced.
data/*
!data/test_sets/
data/test_sets/*
!data/test_sets/*.jsonl
# klareco#932: the parser annotation queues (unreviewed, ~1MB total) were
# gitignored and local-only — exactly the artifact class the June 2026
# migration lost. Small enough to just commit; back them up BEFORE any
# human review time is invested (docs/PARSER_EVALUATION_PROTOCOL.md).
!data/test_sets/parser_pilot_v1/
!data/test_sets/parser_pilot_v2/
!data/test_sets/parser_gold_candidates_v1/
!data/test_sets/discourse_pilot_v1/
models/
logs/
results/

# Temp files
*.tmp
=2.0.0

# OS
.DS_Store
Thumbs.db

# Virtual environments
.venv/
venv/
ENV/
env/

# Generated API documentation
docs/api/
=*
annotations.json
.idlergear.retired/
