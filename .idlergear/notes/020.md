---
id: 20
created: '2026-01-04T02:23:44.671884Z'
tags:
- explore
---
Test Suite Audit - 17 Collection Errors Identified

Ran `pytest tests/ -v` and found 17 import errors. All errors are tests importing non-existent or archived modules.

**CATEGORY 1: Tests for Archived/Deleted Scripts (2 errors)**
- `test_build_corpus_resume.py` → imports `scripts.build_corpus_with_sources` (archived)
- `test_index_corpus_resume.py` → imports `scripts.index_corpus` (archived)
**FIX**: Delete these test files (scripts no longer exist)

**CATEGORY 2: Tests for Future/Unimplemented Modules (9 errors)**
These test modules that will be created in M3/M4:
- `test_generator_model.py` → expects `klareco.models.tree_lstm` (M4 will create)
- `test_corpus_builder.py` → expects `build_corpus_with_sources` (obsolete)
- `test_corpus_cli.py` → expects `klareco.corpus_manager` (not implemented)
- `test_extractive_responder.py` → expects `klareco.experts.extractive` (not implemented)
- `test_intent_classifier.py` → expects `klareco.intent_classifier` (not implemented)
- `test_lang_id.py` → expects `klareco.lang_id` (not implemented)
- `test_orchestrator_extractive.py` → expects `klareco.orchestrator` (not implemented)
- `test_responder.py` → expects `klareco.responder` (not implemented)
- `test_morpheme_aware_embeddings.py` → expects `klareco.embeddings.unknown_tracker` (not implemented)
**FIX**: Mark as `@pytest.mark.skip(reason="Module not implemented yet - M3/M4")` OR delete

**CATEGORY 3: Tests with Outdated Imports (3 errors)**
- `test_retriever.py` → expects `KlarecoRetriever` (actual: `Retriever`)
- `test_run_pipeline_integration.py` → expects `create_retriever()` (doesn't exist)
- `test_structural_index.py` → (import error not shown in truncated output)
**FIX**: Update imports to match current API

**IMPACT**: 371 tests collected but 17 errors = **354 runnable tests**

**RECOMMENDED ACTION**:
1. DELETE 2 obsolete test files (archived scripts)
2. SKIP 9 future-module tests (add @pytest.mark.skip)
3. FIX 3 outdated import tests (update to current API)
4. Then re-run: `pytest tests/ -v --tb=short`

**PRIORITY**: P1 (this week) - need clean test suite before M3/M4 implementation
