---
id: 55
created: '2026-01-05T00:15:41.171559Z'
tags:
- explore
---
## Summary: Session Findings - Sentence Types & Critical Data Gap

### Question Asked
User asked about Esperanto sentence types and whether they need special handling in retrieval.

### Findings

**1. Sentence Types in Esperanto** ✅
- **Parser already extracts** `fraztipo` field in AST
- **Types detected**: deklaro (statement), demando (question), ordono (command)
- **Already being used**: Bug #2 fix uses question detection for partial bonus (0.8 vs 0.5)
- **Partial issue**: Conditional (kondiĉa) detection not working (shows as deklaro)

**2. Current Usage**
```python
# Already implemented in all 6 retrievers:
is_question = query.strip().endswith('?') or query_ast.get('fraztipo') == 'demando'
partial_bonus = 0.8 if is_question else 0.5  # Questions get higher partial match
```

**3. CRITICAL DISCOVERY: Wikipedia Data Gap** ❌
While investigating query failure, found:
- ✅ Wikipedia extraction exists: 2747 articles in `data/extracted/wikipedia_sentences.jsonl`
- ❌ Corpus is MISSING Wikipedia: 0 articles in `data/corpus/unified_corpus.jsonl`
- ❌ Index is MISSING Wikipedia: 0 articles in `data/indexes/slot_full`
- **Impact**: Factual Q&A completely broken (no "L. L. Zamenhof", "Esperanto", "La Espero" articles)

### Root Causes

1. **Wrong corpus builder used**: Old `parse_corpus.py` reads broken `cleaned_wikipedia.txt` (225MB single line)
2. **Correct builder not run**: `build_enhanced_corpus.py` reads proper `wikipedia_sentences.jsonl`
3. **Bug #1 fix not applied**: Corpus built before parser improvements

### Tasks Created

- Task #22: Fix parser conditional sentence detection (P2)
- Task #23: Fix MultiFAISSSlotRetriever API mismatch (P1)
- Task #24: Fix missing unknown_tracker module (P1)
- Task #25: Rebuild corpus with Wikipedia data (P0 - CRITICAL)

### Answers to User's Questions

**Q: Does Esperanto have sentence types?**
A: Yes - deklaro, demando, ordono, kondiĉa (though kondiĉa detection needs fixing)

**Q: Can they be deterministically identified?**
A: Yes, mostly. Parser uses grammar rules (? suffix, verb forms, etc.)

**Q: Should they be in the AST?**
A: Already are! `fraztipo` field

**Q: Do other sentence types need special handling?**
A: Currently only questions need special handling (higher partial bonus). Commands and conditionals probably don't need it for retrieval.

**Q: How does this help retriever performance?**
A: Question detection gives ~60% improvement in partial match scores for factual queries with missing subjects.

### Action Priority

1. **P0**: Rebuild corpus with Wikipedia (task #25) - 4-5 hours
2. **P1**: Fix API bugs (tasks #23, #24) - Quick fixes
3. **P1**: Re-run Q&A benchmark after rebuild
4. **P2**: Fix conditional detection (task #22) - Enhancement
