---
id: 89
created: '2026-01-07T01:12:42.475209Z'
tags:
- pending
- rebuild
- indexing
---
## Pending Index Rebuild Required

After completing HEAD/MODIFIER implementation (#106-#110), need to rebuild indexes:

1. `./scripts/parse_corpus.sh --fresh` - Rebuild corpus with new parser (compound word handling)
2. `./scripts/index_slot.sh --fresh` - Rebuild slot index with HEAD/MOD structure
3. `python scripts/build_mmap_faiss_from_slot_index.py --index-dir data/indexes/slot_hybrid` - Rebuild mmap arrays

**Wait until other AST-aware retriever improvements are done** to avoid multiple rebuilds.
